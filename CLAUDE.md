# CLAUDE.md

## 다른 Claude 계정과 동시 작업 (먼저 읽을 것)

같은 PC 를 Claude 두 계정이 번갈아 쓴다. **이 저장소는 볼트 밖이라 볼트 CLAUDE.md 가 안 읽힌다.** 그래서 같은 규칙을 여기 둔다.

표시판: `H:/내 드라이브/obsidian/Obsidian/settings/agents/작업중.md`

1. **시작할 때** 그 파일을 연다. "지금 작업 중" 표에 이 저장소가 적혀 있으면 건드리지 말고 김호에게 알린다. 없으면 내 행을 적는다
2. **끝날 때** 내 행을 지우고 바꾼 것·끈 것·남은 일을 "인계" 맨 위에 3~5줄 남긴다
3. **시작 시각이 6시간 넘은 행은 죽은 세션으로 본다.** 김호에게 한 줄 알린 뒤 지우고 내 행을 적는다. 안 그러면 한 번 죽을 때마다 표가 영구히 잠긴다
4. **`git pull` 후 수정, 강제 푸시 금지**
5. **긴 세션은 시작 때 한 번만 보면 부족하다.** 작업 덩어리가 바뀔 때 표를 다시 본다

## 이 저장소

볼트 안 `.work-repos/` 에 같은 이름의 사본이 있는데 **옛 상태다.** 코드 작업은 여기서 한다.

관련 기억: `project_two_claude_accounts`
