// These are accessed via CSS variables so they work with both themes
export const chartTheme = {
  grid: "var(--chart-grid)",
  tick: "var(--chart-tick)",
  tooltipBg: "var(--chart-tooltip-bg)",
  tooltipBorder: "var(--chart-tooltip-border)",
  tooltipText: "var(--chart-tooltip-text)",
};
